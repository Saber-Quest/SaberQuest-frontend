/**
 * Enum representing different challenge difficulty levels.
 */
export enum ChallengeDiff {
  /**
   * Normal difficulty level.
   */
  N = "Normal",

  /**
   * Hard difficulty level.
   */
  H = "Hard",

  /**
   * Expert difficulty level.
   */
  Ex = "Expert",
}

/**
 * Enum representing the different platforms that a challenge can be played on.
 */
export enum ChallengePlatforms {
  /**
   * PC platform.
   */
  SS = "ss",

  /**
   * Quest platform.
   */
  BL = "bl",
}

/**
 * Enum representing different challenge types or categories.
 */
export enum ChallengeType {
  /**
   * PP (Performance Points) challenge type.
   */
  PP = "pp",

  /**
   * Map challenge type.
   */
  Map = "map",

  /**
   * FCNotes (Full Combo Notes) challenge type.
   */
  FCNotes = "fcnotes",
  FCN = "fcnotes",

  /**
   * PassNotes challenge type.
   */
  PNotes = "passnotes",
  PN = "passnotes",

  /**
   * FCStars (Full Combo Stars) challenge type.
   */
  FCStars = "fcstars",
  FCS = "fcstars",

  /**
   * XAccuracyStars (X Accuracy Stars) challenge type.
   */
  XAStars = "xaccuracystars",
  XAS = "xaccuracystars",

  /**
   * XAccuracyPP (X Accuracy Performance Points) challenge type.
   */
  XAPP = "xaccuracypp",
  XAP = "xaccuracypp",

  /**
   * XAccuracyNotes (X Accuracy Notes) challenge type.
   */
  XANotes = "xaccuracynotes",
  XAN = "xaccuracynotes",
}
